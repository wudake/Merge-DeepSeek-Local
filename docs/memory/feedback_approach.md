# 轻量方案优先

> 工作偏好 — 优先选择改动小、不影响功能的方案，避免过度工程化

先尝试轻量方案，再考虑重写。

**原则:** 面对改动需求时，先提出一个改动最小的方案（改 CSS、复用已有组件），让用户选择是否升级到完整方案。默认从轻开始，不要一上来就提大重构。

**案例:** UI 统一需求 — 选择"Flask 模板 CSS 更新 + 4 页面 Ant Design 重写"，而非将整个 video-auto 模块重写为 React。
